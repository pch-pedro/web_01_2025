//Criando uma tipo de variável
type Tarefa = {
    descricao: string;
    prioridade: number; 
    concluida: boolean;
}

//Criando uma função
function executarTarefas(ArrayTarefas: Tarefa, ): Tarefa{
    return ArrayTarefas;
}